# Agency backend config.
HERMES_MODE=mock          # 'mock' (fake completions) | 'live' (real engine)
PORT=5003                 # avoid clashing with Octagon IQ on 5002
AGENCY_AUTOPILOT=1        # 1 = trickle of auto jobs keeps the HUD alive; 0 = off

# --- Live Hermes swap: uncomment + fill when the engine is ready ---------
# HERMES_ENDPOINT=http://localhost:11434/v1/chat/completions
# HERMES_API_KEY=
