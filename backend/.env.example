# Copy to .env. Defaults are fine for the mock build.
HERMES_MODE=mock
PORT=5003
AGENCY_AUTOPILOT=1

# Live engine (placeholders for the future swap — read by hermes_adapter.py):
# HERMES_ENDPOINT=http://localhost:11434/v1/chat/completions
# HERMES_API_KEY=
